SQLAlchemy==2.0.43
alembic==1.18.4
psycopg2-binary==2.9.12

fastapi==0.136.1
uvicorn==0.46.0
# Advisory-fixed pin for all four python-multipart findings of the 2026-08-13
# audit (#56): 0.0.30 fixes PYSEC-2026-3036/3037 and CVE-2026-53537
# (GHSA-vffw-93wf-4j4q, Content-Disposition parameter smuggling); 0.0.31 fixes
# PYSEC-2026-3040 (negative Content-Length buffering).
python-multipart==0.0.31
# WSGI adapter for the cPanel/Passenger staging host only (passenger_wsgi.py).
# Production runs uvicorn directly and never imports it.
a2wsgi==1.10.10

pydantic==2.13.4
pydantic-settings==2.14.2  # 2.14.2 fixes GHSA-4xgf-cpjx-pc3j
pydantic_core==2.46.4
email-validator

# Pulls in `ecdsa`, which carries PYSEC-2026-1325 / CVE-2024-23342 (Minerva
# timing side-channel on ECDSA signing) with no fixed release in any version,
# latest (0.19.2) included. Not exercised here: JWT_ALGORITHM is HS256 (HMAC,
# symmetric) and no EC key is ever signed with — see app/lib/config.py.
# The GitHub-side scan (.github/workflows/security.yml) ignores that ID; GitLab
# Dependency Scanning still reports it (tracked in #56). The real fix is
# replacing python-jose with PyJWT, which drops `ecdsa` from the tree — see #89.
python-jose==3.5.0
passlib==1.7.4
bcrypt
boto3==1.43.6
botocore==1.43.6

python-dotenv==1.2.2
python-json-logger
tenacity
redis==5.2.1

reportlab>=4.0
openpyxl>=3.1

# OpenTelemetry metrics (activated via OTEL_METRICS_ENABLED; see app/common/otel.py)
opentelemetry-api>=1.30
opentelemetry-sdk>=1.30
opentelemetry-exporter-otlp-proto-http>=1.30

pytest==9.0.3
pytest-asyncio==1.3.0
httpx==0.28.1
ruff==0.15.12
mypy
