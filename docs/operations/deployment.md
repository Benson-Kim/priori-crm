| `STAGING_SSH_USER/HOST`, `PROD_SSH_USER/HOST` | GitHub Actions secrets | Not really secret, but keeps hosts out of the YAML |
| `DRILL_SSH_KEY`, `DRILL_KNOWN_HOSTS`, `DRILL_SSH_USER`, `DRILL_SSH_HOST` | GitHub Actions secrets | **Per-drill throwaway values** for `deploy-drill.yml` (DR drill Variant A). A human provisions them right before a drill — fresh keypair, `ssh-keyscan` of the new droplet — and deletes them with the droplet (backup runbook, drill Variant A). Unset the rest of the year: the drill guard then fails with a clear message instead of deploying anywhere. Never reuse production values here — the guard refuses any host that is, or resolves to, production |