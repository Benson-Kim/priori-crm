# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
htmlcov/
coverage.xml
.coverage

# Node / UI
node_modules/
dist/
build/
.vercel/

# Generated API contract types (produced from the OpenAPI schema by
# `npm run gen:api` / CI; never hand-edited or committed).
frontend/src/lib/api-schema.d.ts
# Transient schema artifact exported at the repo root during CI. The
# committed, build-time copy lives at frontend/openapi.json (NOT ignored)
# so Vercel can read it without a Python runtime.
/openapi.json

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
*.swp
*.swo

# Docker
docker-compose.override.yml

# Runtime user uploads
api/uploads/

# Misc
*.log